name: CS-to-SQL
repo_root: /mnt/data/lancer1977/code/CS-to-SQL

languages:
  - csharp
  - markdown
  - yaml
frameworks:
  - dotnet
  - docs
  - docker

ci:
  build: []
  test:
    - bash scripts/validate.sh

devstudio:
  runtime_model: three-agent
  backlog_ordering: filename
  skip_promoted_planning: true
  one_stage_per_run: true
